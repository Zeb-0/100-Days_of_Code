// expand arrays & other expressions in places -
// where multiple elements are expected.

/* copy arr2 to arr1 using spread op. */
const arr1 = ['JAN', 'FEB', 'MAR', 'APR', 'MAY'];
let arr2;

arr2 = [...arr1];
console.log(arr2);
