const add = (x, y) => {
    return x + y;
}

const sub = (x, y) => {
    return x - y;
}

const mul = (x, y) => {
    return x * y;
}

const square = (x, y) => {
    return x ** y;
}

const div = (x, y) => {
    return x / y;
}

export { add, sub, square, mul, div };